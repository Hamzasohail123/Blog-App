const conf = {
    appwriteUrl: 
    String(import.meta.env.REACT_APP_APPWRITE_URL),
    projectId: 
    String(import.meta.env.REACT_APP_PROJECT_ID),
    collectionId:
     String(import.meta.env.REACT_APP_COLLECTION_ID),
    databaseId: 
    String(import.meta.env.REACT_APP_DATABASE_ID),
    storageBucket:
    String(import.meta.env.REACT_APP_STORAGE_BUCKET),
}

export default conf